<% layout('layout') -%>

<div class ="account-form-container">
    <form class="col s6 login-form">
        <h3>Manage Account </h3>
    </form>
</div>

<!-- <div style="display: flex; justify-content: space-between; align-items: center;">
    <h3>User Points</h3>
    <span id="userPoints" style="font-size: 18px; font-weight: bold; color: #007bff;">0 pts</span>
</div> -->

<script>
    async function fetchUserPoints() {
        try {
            const response = await fetch('/account/points');
            if (!response.ok) throw new Error('Failed to fetch points');
            const data = await response.json();
            document.getElementById('userPoints').innerText = `${data.points} pts`;
        } catch (error) {
            console.error('Error fetching user points:', error);
        }
    }

    // Fetch points on page load
    fetchUserPoints();
</script>
